def review_changes(changes_needed):
    # Allow users to interactively review and confirm individual changes before applying them

def modify_desired_state():
    # Implement an interactive mode that allows users to modify the desired state during runtime

def confirm_apply_changes():
    # Ask the user for confirmation before applying changes
    # Return True if the user confirms, False otherwise
